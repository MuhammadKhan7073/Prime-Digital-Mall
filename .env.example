# Prime Digital Mall runs with NO environment variables required.
# This file is a placeholder for future integrations.
#
# Optional overrides:
# PORT=3000                      # port for `npm start`
#
# When you add a real backend / payment gateway later, put secrets here
# and read them via process.env (never commit the filled-in .env).
